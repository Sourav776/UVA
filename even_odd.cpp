#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    while(cin>>n)
    {
        //cout<<(n^1)<<endl;
            while((n^1)) {cout<<"Even"<<endl;break;}
            while(!(n^1)) {cout<<"Odd"<<endl;break;}

    }
    return 0;
}
